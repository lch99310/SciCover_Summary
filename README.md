# SciCover

**全球頂級科學期刊封面故事 · 雙語解讀**
*Top Science Journal Cover Stories · Bilingual Summaries*

---

## 為什麼需要 SciCover？

每週，Science、Nature、Cell 等頂級期刊都會發表突破性的封面研究，但普通讀者很難觸及這些內容：

- **語言門檻**：論文全是英文，專業術語密集，非母語讀者難以消化
- **資訊分散**：要追蹤多本期刊，需要在不同網站之間反覆切換
- **內容艱澀**：即使看得懂英文，學術寫作的風格也讓一般人望而卻步
- **時間成本**：每週手動查看各期刊官網、找出封面故事，耗時費力
- **缺乏中文科普**：市面上幾乎沒有即時、高品質的中文科學封面故事解讀

**SciCover 解決這些問題。** 它自動抓取頂級期刊的封面故事，用 AI 生成中英雙語的通俗解讀，讓任何人都能在一個網站上輕鬆了解最前沿的科學進展。

---

## 功能特色

### 🔬 自動追蹤三大期刊
每週自動抓取 **Science**、**Nature**、**Cell** 的最新封面故事，無需手動檢查。

### 🌐 中英雙語摘要
AI 生成的摘要不是逐字翻譯，而是面向大眾的「意譯式改寫」——中文讀者能讀懂，英文讀者也有原味解讀。

### 📱 現代化閱讀體驗
仿照 Quanta Magazine 的雜誌風格設計，支援：
- **期刊篩選**：一鍵切換 Science / Nature / Cell 或查看全部
- **深色模式**：白天黑夜都舒適閱讀
- **響應式佈局**：手機、平板、桌面都完美呈現
- **圖片展示**：封面圖與文內配圖，附帶雙語圖說

### 📅 歷史歸檔
按年月瀏覽過往的封面故事，建立你的科學閱讀時間軸。

### 💰 零成本運行
全站部署在 GitHub Pages，使用 GitHub Models API 的免費額度產生摘要，不需要任何伺服器或付費服務。

---

## 線上預覽

部署後的網址格式：
```
https://<你的GitHub帳號>.github.io/<repo名稱>/
```

---

## 快速開始

### 前端本地開發

```bash
# 1. 複製專案
git clone https://github.com/<owner>/scicover.git
cd scicover

# 2. 安裝前端依賴
cd frontend
npm install

# 3. 建立資料軟連結
ln -s ../data   public/data
ln -s ../images public/images

# 4. 啟動開發伺服器
npm run dev
```

打開 **http://localhost:5173** 即可預覽。

### 執行爬蟲（本地）

```bash
pip install -r scripts/requirements.txt

# 設定 API Token（需要 GitHub Models API 存取權限）
export GITHUB_TOKEN=your_pat_here

# 抓取所有期刊
python -m scripts.main

# 只抓取特定期刊
python -m scripts.main --journal Science --journal Nature

# 測試模式（只爬取，不呼叫 AI）
python -m scripts.main --journal Nature --dry-run
```

---

## 技術架構

```
┌──────────────────────────────────────────────────────────┐
│                    GitHub Actions                         │
│                                                          │
│  ┌─────────────────────┐      ┌───────────────────────┐  │
│  │ scrape-and-summarize │      │       deploy          │  │
│  │   (每週六 06:00 UTC) │      │  (push 到 main 時)    │  │
│  │                      │      │                       │  │
│  │  1. 爬取期刊封面     │─────▶│  1. npm ci && build   │  │
│  │  2. AI 生成摘要      │ push │  2. 複製 data/images  │  │
│  │  3. 提交 data/       │      │  3. 部署到 Pages      │  │
│  │     & images/        │      │                       │  │
│  └─────────────────────┘      └───────────────────────┘  │
└──────────────────────────────────────────────────────────┘
                                          │
                                          ▼
                                   GitHub Pages
                                 (靜態 Vite SPA)
```

### 技術棧

| 層級 | 技術 |
|------|------|
| 前端 | React 19 + Vite 6 + TypeScript |
| 樣式 | 自定義 CSS（無 UI 框架），Playfair Display + Noto Serif SC 字型 |
| 動畫 | Framer Motion |
| 資料 | @tanstack/react-query，靜態 JSON 檔案 |
| 後端 | Python 3.12（requests + BeautifulSoup） |
| AI | GitHub Models API + DeepSeek V3 |
| 部署 | GitHub Pages（免費） |
| CI/CD | GitHub Actions |

### 專案結構

```
scicover/
├── .github/workflows/          # CI/CD 自動化
│   ├── scrape-and-summarize.yml  # 每週自動爬取 + AI 摘要
│   └── deploy.yml                # 自動部署到 GitHub Pages
├── frontend/                   # React 前端
│   └── src/
│       ├── components/           # UI 元件
│       ├── hooks/                # React Hooks
│       ├── lib/                  # 型別定義 + 工具
│       └── styles/               # CSS 設計系統
├── scripts/                    # Python 後端
│   ├── scraper/                  # 各期刊爬蟲
│   ├── ai/                       # AI 摘要產生器
│   ├── pipeline/                 # 執行流程管理
│   └── main.py                   # CLI 進入點
├── data/                       # 靜態 JSON 資料
│   ├── articles/                 # 按年/月分類的文章
│   ├── index.json                # 文章索引
│   └── latest.json               # 各期刊最新文章
└── images/                     # 封面圖片
```

### 資料格式

每篇文章以 JSON 儲存，包含：

```jsonc
{
  "id": "science-2026-02-26",
  "journal": "Science",
  "date": "2026-02-26",
  "coverImage": { "url": "...", "alt": { "zh": "...", "en": "..." } },
  "title": { "zh": "中文標題", "en": "English Title" },
  "summary": { "zh": "中文摘要（800-1200字）", "en": "English summary" },
  "keyArticles": [{ "title": "...", "authors": "...", "doi": "..." }],
  "images": [{ "url": "...", "caption": { "zh": "...", "en": "..." } }]
}
```

---

## GitHub Actions 設定

### 1. 爬取與摘要 (`scrape-and-summarize.yml`)

| 項目 | 說明 |
|------|------|
| 觸發 | 每週六 06:00 UTC，或手動觸發 |
| 功能 | 執行 Python 爬蟲，AI 生成摘要，自動提交到 repo |
| 密鑰 | `MODELS_PAT`：GitHub PAT，需有 Models API 存取權限 |

### 2. 部署 (`deploy.yml`)

| 項目 | 說明 |
|------|------|
| 觸發 | push 到 main 且涉及 `frontend/**`、`data/**` 或 `images/**` |
| 功能 | 建置 Vite 應用，打包資料與圖片，部署到 GitHub Pages |

---

## 新增期刊

1. 在 `scripts/scraper/` 建立新的爬蟲模組（例如 `lancet_scraper.py`）
2. 繼承 `BaseScraper`，實作 `scrape()` 方法
3. 在 `pipeline/runner.py` 的 `SCRAPER_MAP` 中註冊
4. 在 `frontend/src/lib/constants.ts` 的 `JOURNALS` 陣列中新增
5. 本地測試：`python -m main --journal Lancet`
6. 合併 PR 後，每週排程會自動開始處理

---

## License

[MIT](LICENSE)
